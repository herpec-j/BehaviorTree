os:
  - Visual Studio 2015 CTP
branches:
  only:
    - master
before_build:
  - mkdir Build
  - cd Build
  - cmake .. -G "Visual Studio 14"
build_script:
  - msbuild BehaviorTree.sln /nologo /verbosity:minimal
test_script:
  - msbuild RUN_TESTS.vcxproj /nologo /verbosity:minimal
